interface Product {
  id: number;
  name: string;
  barcode: string;
  price: number;
  discountPrice: number;
  memberPrice: number;
  description: string;
  imageBase64: string;
}

export default Product;
